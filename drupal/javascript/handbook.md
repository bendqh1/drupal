## Add a modal for nodes in the JavaScript file

```js
const classNames = document.querySelector("body").classList;
if (
    classNames.contains("path-node")
) {
    // HTML-JavaScript-CSS structure code comes here.
}
```
